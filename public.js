'use strict';
let Base = require('./base');

/**
 * @brief:
 *
 * @author:   lcy
 * @date:     2019/9/19 20:56
 */

global.Public = function (file, filePath, opts) {
    Base.call(file, filePath, opts);
}